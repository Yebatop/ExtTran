/**
 * Имя и номер главы — в браузере.
 *
 * Модуль чистый: ни файлов, ни сети, ни ключа, только разбор адреса. Поэтому
 * его можно тащить в клиентскую сборку, в отличие от остального ядра, которое
 * живёт только на сервере. Правило наименования при этом остаётся одно на оба
 * берега — второй реализации, которая однажды разойдётся с первой, здесь нет.
 */

export { chapterName, chapterNumber } from "../../dist/book.js";
