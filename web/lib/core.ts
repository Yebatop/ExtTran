/**
 * Дверь в ядро.
 *
 * Ядро лежит выше по дереву и собирается отдельным `npm run build:core`:
 * это обычный ESM-JavaScript, который сайт исполняет на сервере, а не тащит
 * в свою сборку. Единственный файл, который знает про эту границу, — вот этот.
 */

export * from "../../dist/index.js";
