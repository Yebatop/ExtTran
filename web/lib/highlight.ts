/**
 * Подсветка терминов — в браузере.
 *
 * Модуль чистый: ни файлов, ни сети, ни ключа. Поэтому его можно тащить в
 * клиентскую сборку, в отличие от остального ядра, которое живёт только на
 * сервере. Разбор при этом остаётся один на оба берега — второй реализации,
 * которая однажды разойдётся с первой, здесь нет.
 */

export { highlight, type HighlightTerm, type Segment } from "../../dist/highlight.js";
