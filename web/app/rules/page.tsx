import Link from "next/link";

/**
 * Пока страница статическая, браузер держит её HTML между развёртываниями —
 * и человек видит вчерашнюю форму. Выглядит это не как устаревание, а как
 * поломка: на телефоне так пропало поле выбора модели, добавленное часом
 * раньше, и два перевода подряд ушли не на ту модель.
 *
 * Мы правим сайт по несколько раз в час, и на этом этапе «что вижу — то и
 * развёрнуто» стоит дороже сэкономленных миллисекунд. Перед запуском, когда
 * страница перестанет меняться каждый час, это надо вернуть обратно.
 */
export const dynamic = "force-dynamic";

/**
 * Правила. Коротко и без юридического тумана: человек должен понять за минуту,
 * что происходит с чужим текстом и с его собственным переводом.
 *
 * Полная версия решений — docs/rights-policy.md в репозитории. Её писал не
 * юрист, и до запуска она должна пройти через юриста.
 */
const POINTS: Array<[string, string]> = [
  [
    "Перевод делается для вас",
    "Вы дали ссылку — мы перевели главу вам. По умолчанию этот перевод видите только вы.",
  ],
  [
    "Выкладывать или нет — решаете вы",
    "Захотите поделиться — перевод появится под вашим именем. Не захотите — он останется вашим и через сто восемьдесят дней после последнего открытия будет удалён.",
  ],
  [
    "Оригинал мы не храним",
    "Английский текст нужен ровно на время перевода. Копии у себя мы не держим.",
  ],
  [
    "Платные главы не переводим",
    "Если глава куплена у автора или переводчика, наше дело туда не лезть. Мы такие страницы узнаём и отказываемся.",
  ],
  [
    "Поиску отдаём только своё",
    "Карточки книг, глоссарии и страницы переводчиков — да. Тексты переводов — нет.",
  ],
  [
    "Обращение правообладателя",
    "Три рабочих дня на ответ и открытый список того, что было изъято.",
  ],
];

export default function Rules() {
  return (
    <main style={{ maxWidth: 660, margin: "0 auto", padding: "clamp(28px, 7vw, 80px) 16px 96px" }}>
      <nav style={{ marginBottom: 40, fontSize: 13 }}>
        <Link href="/">← На главную</Link>
      </nav>

      <h1 className="display" style={{ fontSize: "clamp(30px, 7vw, 44px)", margin: "0 0 16px", lineHeight: 1.1 }}>
        Что мы делаем с чужим текстом
      </h1>
      <p style={{ color: "#c8bfae", lineHeight: 1.65, margin: "0 0 48px", fontSize: 17 }}>
        Перевод — производное произведение, и у оригинала есть автор. Вот как мы
        с этим обходимся.
      </p>

      <div style={{ display: "grid", gap: 32 }}>
        {POINTS.map(([title, body]) => (
          <section key={title}>
            <h2 style={{ fontSize: 17, margin: "0 0 7px", fontWeight: 600 }}>{title}</h2>
            <p style={{ margin: 0, color: "#9a917f", lineHeight: 1.65, fontSize: 15 }}>{body}</p>
          </section>
        ))}
      </div>

      <p
        style={{
          marginTop: 56,
          paddingTop: 24,
          borderTop: "1px solid var(--line-soft)",
          color: "var(--dim)",
          fontSize: 13,
          lineHeight: 1.6,
        }}
      >
        Эту страницу писал не юрист. До запуска её нужно показать юристу — список
        вопросов лежит в репозитории рядом с полной версией правил.
      </p>
    </main>
  );
}
